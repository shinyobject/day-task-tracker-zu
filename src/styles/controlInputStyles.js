import { css } from "styled-components";

export const controlInputStyles = css`
  label {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  input {
    width: 3ch;
    padding: 4px;
    border: 1px solid black;
  }
`;
